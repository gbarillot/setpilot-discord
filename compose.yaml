services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - ./.env:/home/python/.env:ro
      - ./logs:/home/python/logs
    networks:
      - setpilot-net
    restart: unless-stopped

networks:
  setpilot-net:
    external: true
